title: Daily Digest
description: AI, dev, and homelab news — one page a day.
theme: jekyll-theme-primer
show_downloads: false

# Pages serves this folder (Settings -> Pages -> Source: main /docs).
# Nothing here needs plugins; index.md is regenerated by the digest routine.
defaults:
  - scope:
      path: ""
    values:
      layout: default
