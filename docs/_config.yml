title: Daily Digest
description: AI, dev, and homelab news — one page a day.

# No remote theme: the layout in _layouts/default.html owns the styling, which is
# dark and tuned for reading on a phone. A theme would only be fought with overrides.
defaults:
  - scope:
      path: ""
    values:
      layout: default
